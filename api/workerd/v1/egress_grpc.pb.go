// SPDX-License-Identifier: AGPL-3.0-only
//
// EgressConfig is the worker-side sink of the workerd egress config plane
// (APO-723). The backplane's ServiceReconciler (APO-726) compiles egress
// routing/policy per Service and pushes it here over a per-tenant localhost
// listener (a filesystem-permissioned unix domain socket); the manager fans it
// out to the sandbox EgressController live setters
// (SetEgressBackends/SetEgressPolicy/SetInvocationID).
//
// This gRPC drives the WORKER bridge's setters only, never the Sentry: there
// is no general live urpc into the running sandbox. Applies are idempotent and
// last-writer-wins, ordered by generation.

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             v5.28.3
// source: egress.proto

package workerdv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	EgressConfig_ApplyEgress_FullMethodName = "/apoxy.workerd.v1.EgressConfig/ApplyEgress"
)

// EgressConfigClient is the client API for EgressConfig service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// EgressConfig applies compiled egress configuration to a tenant's resident.
type EgressConfigClient interface {
	// ApplyEgress installs the full egress config for one sandbox: the
	// EgressGateway backend listeners it may dial, its authorization policy,
	// and the invocation id stamped on egress connections for attribution.
	// Idempotent; a request whose generation is older than the last applied
	// one is ignored (the response echoes the retained generation).
	ApplyEgress(ctx context.Context, in *ApplyEgressRequest, opts ...grpc.CallOption) (*ApplyEgressResponse, error)
}

type egressConfigClient struct {
	cc grpc.ClientConnInterface
}

func NewEgressConfigClient(cc grpc.ClientConnInterface) EgressConfigClient {
	return &egressConfigClient{cc}
}

func (c *egressConfigClient) ApplyEgress(ctx context.Context, in *ApplyEgressRequest, opts ...grpc.CallOption) (*ApplyEgressResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ApplyEgressResponse)
	err := c.cc.Invoke(ctx, EgressConfig_ApplyEgress_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// EgressConfigServer is the server API for EgressConfig service.
// All implementations must embed UnimplementedEgressConfigServer
// for forward compatibility.
//
// EgressConfig applies compiled egress configuration to a tenant's resident.
type EgressConfigServer interface {
	// ApplyEgress installs the full egress config for one sandbox: the
	// EgressGateway backend listeners it may dial, its authorization policy,
	// and the invocation id stamped on egress connections for attribution.
	// Idempotent; a request whose generation is older than the last applied
	// one is ignored (the response echoes the retained generation).
	ApplyEgress(context.Context, *ApplyEgressRequest) (*ApplyEgressResponse, error)
	mustEmbedUnimplementedEgressConfigServer()
}

// UnimplementedEgressConfigServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedEgressConfigServer struct{}

func (UnimplementedEgressConfigServer) ApplyEgress(context.Context, *ApplyEgressRequest) (*ApplyEgressResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ApplyEgress not implemented")
}
func (UnimplementedEgressConfigServer) mustEmbedUnimplementedEgressConfigServer() {}
func (UnimplementedEgressConfigServer) testEmbeddedByValue()                      {}

// UnsafeEgressConfigServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to EgressConfigServer will
// result in compilation errors.
type UnsafeEgressConfigServer interface {
	mustEmbedUnimplementedEgressConfigServer()
}

func RegisterEgressConfigServer(s grpc.ServiceRegistrar, srv EgressConfigServer) {
	// If the following call panics, it indicates UnimplementedEgressConfigServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&EgressConfig_ServiceDesc, srv)
}

func _EgressConfig_ApplyEgress_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ApplyEgressRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(EgressConfigServer).ApplyEgress(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: EgressConfig_ApplyEgress_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(EgressConfigServer).ApplyEgress(ctx, req.(*ApplyEgressRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// EgressConfig_ServiceDesc is the grpc.ServiceDesc for EgressConfig service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var EgressConfig_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "apoxy.workerd.v1.EgressConfig",
	HandlerType: (*EgressConfigServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "ApplyEgress",
			Handler:    _EgressConfig_ApplyEgress_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "egress.proto",
}
